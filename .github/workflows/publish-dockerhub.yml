name: Publish Docker image to Docker Hub

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    name: Build and push Docker image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/fastapi-app:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/fastapi-app:${{ github.sha }}
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/fastapi-app:cache
          cache-to: type=inline

      - name: Show pushed images
        run: |
          echo "Pushed images:"
          echo "${{ secrets.DOCKERHUB_USERNAME }}/fastapi-app:latest"
          echo "${{ secrets.DOCKERHUB_USERNAME }}/fastapi-app:${{ github.sha }}"
